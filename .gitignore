node_modules/
dist/
.next/
out/
*.tsbuildinfo

# Prisma / SQLite
*.db
*.db-journal
apps/server/prisma/dev.db*

# The on-disk context repo is generated by the seed script
apps/server/.context-repo/

# Env — local-only overrides are ignored; the committed .env holds non-secret
# defaults (local SQLite path, ports) so the prototype runs after clone.
.env.local
.env.*.local

# Logs / OS
*.log
.DS_Store

# Disposable working clones for remote workspaces
apps/server/.workspaces/
