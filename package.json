{
  "name": "context-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Context Studio — a Context Management IDE for non-technical Context Owners",
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" run dev",
    "dev:web": "pnpm --filter @context-studio/web dev",
    "dev:server": "pnpm --filter @context-studio/server dev",
    "build": "pnpm --filter \"./packages/*\" run build && pnpm --filter \"./apps/*\" run build",
    "typecheck": "pnpm -r typecheck",
    "seed": "pnpm --filter @context-studio/server seed",
    "db:migrate": "pnpm --filter @context-studio/server exec prisma migrate deploy",
    "bootstrap": "pnpm install && pnpm --filter @context-studio/server prisma:generate && pnpm db:migrate && pnpm seed"
  },
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@prisma/client", "@prisma/engines", "prisma", "esbuild"]
  }
}
